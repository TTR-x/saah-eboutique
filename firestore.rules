rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Default: anyone can read, only authenticated users can write.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Contact messages: anyone can create.
    match /contact-messages/{messageId} {
      allow create: if true;
    }

    // Testimonials: anyone can create.
    match /testimonials/{testimonialId} {
      allow create: if true;
    }
    
    // Slides: only authenticated users can manage.
    match /slides/{slideId} {
        allow read: if true;
        allow create, update, delete: if request.auth != null;
    }

    // Products: Temporarily allow public write for testing.
    // ⚠️ This is for debugging only.
    match /products/{productId} {
      allow read: if true;
      allow write: if true; // Temporary rule for testing
    }

    // Product reviews: anyone can create.
    match /products/{productId}/reviews/{reviewId} {
      allow create: if true;
      // Authenticated users can manage their own reviews (optional future enhancement)
      allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    }
  }
}
