rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Fonctions utilitaires
    function isAdmin() {
      return request.auth != null && request.auth.token.email == "saahbusiness2026@gmail.com";
    }

    // Accès public en lecture pour le contenu du site
    match /products/{productId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /slides/{slideId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    match /testimonials/{testimonialId} {
      allow read: if true;
      allow create: if true; // N'importe qui peut laisser un avis
      allow update, delete: if isAdmin();
    }

    // Autoriser tout le monde à envoyer des messages, mais seul l'admin peut les lire
    match /contact-messages/{messageId} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }

    // Autoriser tout le monde à passer une commande d'importation
    match /import-orders/{orderId} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }

    // Règle par défaut : refuser tout le reste
    match /{document=**} {
      allow read, write: if isAdmin();
    }
  }
}