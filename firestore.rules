rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default: Deny all reads and writes
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Products: Public read, only authenticated write
    match /products/{productId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }

    // Product reviews: Public read, any user can create
    match /products/{productId}/reviews/{reviewId} {
      allow read: if true;
      allow create: if true; // Or `if request.auth != null` if only logged in users can review
    }
    
    // Slides: Public read, only authenticated write
    match /slides/{slideId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }

    // Testimonials: Public read, any user can create
    match /testimonials/{testimonialId} {
      allow read: if true;
      allow create: if true;
    }

    // Contact Messages: Public read for admin, any user can create
    match /contact-messages/{messageId} {
      allow read, update, delete: if request.auth != null; // Only admin should access
      allow create: if true;
    }
  }
}
